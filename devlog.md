# devlog

이 저장소가 지금 형태에 이른 경위. **무엇을 왜 바꿨는지**와 **틀렸던 판단**을 남긴다.
현재 상태는 `README.md`, 작업 규칙은 `CLAUDE.md`에 있고, 여기에는 그 두 문서가 담지 않는 이유와 시행착오만 적는다.

---

## 1. 넓은 탐색 → 좁고 깊은 해부 (~2026-08-07)

처음에는 논문 §4.2의 baseline 7개를 8문항 / 80문단에서 전부 구현했다. 목적은 **각 방법이 실제로 어떤 artifact를 만드는지**를 눈으로 보는 것이었고, 방법 순위를 내려는 것이 아니었다.

그 결과 방법별 산출물의 성격 차이는 파악됐지만, 문항 수가 적고 corpus도 임의로 자른 것이라 **실패가 방법 탓인지 데이터 탓인지 분리되지 않았다.** 그래서 방향을 바꿨다.

- 데이터를 **공식 2WikiMultihopQA validation / MuSiQue-Ans dev**에서 다시 뽑았다 (`tools/fetch_*.py`).
- 비교 대상을 **LLM-Wiki · BM25 · Dense · GraphRAG** 네 개로 좁혔다. 나머지 구현은 남겨뒀지만 현재 slice에서는 실행하지 않았다.
- 판정을 정답률 하나가 아니라 **정답 → 근거 회수 → build/trace** 순서로 보게 했다 (`CLAUDE.md`의 실패 판정 5단계).

이때 초기 7-arm 결과는 폐기가 아니라 **과거 snapshot**으로 분리했다. 현재 데이터·모델과 조건이 다르므로 결과의 근거로 인용하지 않는다.

## 2. 논문 구조에 맞춘 디렉터리 재편 (2026-08-14)

파일이 역할별(`indexing/`, `retrieval/`, `harness/`)로 흩어져 있어서, **논문의 어느 단계를 보는 중인지가 폴더에서 읽히지 않았다.** 논문 구성을 그대로 따르도록 바꿨다.

```
indexing/ retrieval/ harness/  →  baseline/b0~b6 + baseline/llm_wiki/
llm.py embed.py evaluate.py    →  core/          (전 arm 공유 = §4.4 통제)
```

번호 `b0`~`b6`은 논문 §4.2 순서다. 제안 방법만 번호 없이 `baseline/llm_wiki/`로 둬서 기준선과 구분했다.

처음에는 폴더 이름을 `arms/`로 했는데, "arm"은 대조 실험에서 비교 조건을 부르는 말이라 **코드 폴더 이름으로는 뜻이 통하지 않았다.** `baseline/`으로 바꿨다. 다만 결과 JSON의 키와 코드 주석에는 `arm`이 그대로 남아 있다 — 그쪽에서는 "비교 조건"이라는 원래 뜻이 맞기 때문이다.

### 재편이 남긴 흔적

폴더를 옮기면서 **경로를 파일 위치 기준으로 계산하던 코드들이 같이 딸려갔다.**

| 대상 | 증상 |
|---|---|
| `.env` (API 키) | 루트를 못 찾아 **즉시 크래시** → 바로 발견·수정 |
| `runs/cache/` | `core/runs/cache/`에 새로 생성. **에러 없이** 빈 캐시로 재시작 |
| ErrorBook | `arms/` 문자열이 코드에 남아, 없어진 폴더에 계속 기록 |

같은 실수인데 하나는 시끄럽게, 둘은 조용히 망가졌다. 조용한 쪽은 `--dry-run`으로도 안 잡혔다 — dry-run은 첫 API 호출 직전에 멈추는데, 문제가 그 지점에 있었거나 아예 에러를 내지 않았기 때문이다.

→ 이후 **경로는 프로젝트 루트 기준으로 계산**한다 (`CLAUDE.md` 파일·실행 규칙).

## 3. 모델과 임베딩 교체 (2026-08-14)

- 생성 모델을 `claude-sonnet-5`로 통일했다. 논문 §4.4대로 **build와 answer에 같은 모델**을 쓴다.
- 임베딩을 `all-MiniLM-L6-v2` → `Qwen/Qwen3-Embedding-0.6B`로 바꿨다. 논문은 8B를 쓰지만 로컬 실행이 가능한 0.6B로 대체했고, 이 차이는 `README.md` 해석 경계에 적어뒀다.

교체 이유는 성능 일반론이 아니라 **구분 마진**이었다. `John V` vs `John VI` 같은 표기가 2Wiki에 자주 나오는데, MiniLM도 순위 자체는 맞히지만 두 후보의 점수 차가 0.022로 얇았다. Qwen은 0.095였다. 얇은 마진은 top-k 경계에서 뒤집히기 쉽다.

임베딩은 **문서 쪽 벡터가 (텍스트, 모델)만의 함수**라 여러 arm이 같은 계산을 반복했다. `core/embed.py`에 캐시(`encode_cached`)를 넣어 arm 간에 재사용하게 했다.

## 4. 버그 수정 (2026-08-14)

### LightRAG의 hop 누수

LightRAG는 엔티티를 찾은 뒤 **정확히 1홉**만 확장하는 것이 설계다. 커뮤니티 요약 없이 싸게 간다는 것이 이 방법의 논지이기 때문이다. 그런데 확장 루프가 **결과 집합을 다시 조건으로 읽고 있어서**, 앞에서 추가된 노드가 뒤 관계에 다시 걸려 계속 뻗어나갔다.

측정: 수정 전 hop 분포 `{0:5, 1:13, 2:7, 3:3, 4:1}` → 수정 후 `{0:5, 1:13}`.

이 누수 덕에 맞히던 문항이 있었다. 즉 **버그가 만든 정답**이었고, 방법의 성질로 해석하면 안 되는 결과였다.

### 요약 잘림 (RAPTOR / GraphRAG)

두 방법 모두 요약을 만들 때 출력 상한이 고정값(700 토큰)이었다. 커뮤니티 크기는 34명부터 1명까지 편차가 큰데 상한이 같으니 **큰 커뮤니티만 정확히 잘렸다.** 그리고 다중 엔티티 질문이 사는 곳이 바로 그 큰 커뮤니티였다.

RAPTOR 요약 20개 중 9개, GraphRAG 리포트 43개 중 2개가 문장 중간에서 끊겼다. GraphRAG 쪽 2개가 **가장 큰 커뮤니티 두 개**였다. 질의 시점에는 리포트만 읽으므로 잘린 사실은 복구 불가능했다.

→ 입력 길이에 비례하는 예산으로 바꾸고, 끝 문자가 문장부호가 아니면 빌드 중 경고를 찍게 했다.

처음에는 이 현상을 "요약이 사실을 버린다"고 해석했는데 **틀렸다.** 요약 판단의 문제가 아니라 단순 상한 초과였다.

### 토크나이저가 비ASCII를 버림

BM25와 LLM-Wiki의 검색용 토크나이저가 `[a-z0-9]+`였다. 16문항 중 **15문항의 gold에 비ASCII 문자가 있어서**(`Żuławski`, `Ernst`, 악센트 붙은 인명) 검색 표면에서 통째로 사라졌다. 한국어는 완전히 무너진다 — `방송통신발전기금의 징수율은 별표 2와 같다` → `['2']`.

임베딩 계열은 이 문제가 없어서, **어휘 검색 arm만 일방적으로 불리한** 비교가 되고 있었다. `\w+` (UNICODE)로 교체했다.

## 5. 저장소 정리 (2026-08-15)

교수님과 구조를 공유하기 위해 커밋을 준비하며 정리했다.

- 캐시를 `runs/cache/`로 되돌리고(파일 1,853개 이동, 유실 0), `core/runs/` 삭제.
- ErrorBook을 `indexes_<dataset>/llm_wiki/error_book.yaml`로 옮겼다. **빌드 산출물이므로 인덱스와 같은 곳**에 있는 것이 맞고, dataset별 폴더로 들어가면서 파일명의 `_2wiki`/`_musique` 접미사도 필요 없어졌다. `arms/` 폴더는 이걸로 사라졌다.
- `REPORT_*.md` 생성 위치를 레포 밖 → `runs/`로 바꿨다.
- 커밋 기준을 정했다: **LLM이 만든 산출물은 커밋, 재생성이 공짜인 것은 제외.** `*.npz`·`runs/cache/`·`runs/history/`를 `.gitignore`에 추가했다. history는 "과거 실행 보관"이라는 역할이 git과 겹친다.
- 문서가 레포 밖 파일을 주 문서로 걸고 있어서 링크가 깨져 있었다. 걷어내고 **저장소가 자체 완결되도록** 고쳤다.

## 앞으로

- LightRAG·RAPTOR·HippoRAG는 구현만 있고 현재 slice에서 실행하지 않았다. 같은 수준으로 해석하려면 다시 돌려야 한다.
- 2Wiki 5문항 / MuSiQue 3문항은 **탐색 사례**다. 이 수로는 방법의 일반 성능 순위를 주장할 수 없다.
- MuSiQue m2·m3은 gold chain이 원문에 grounded되는지부터 별도로 확인해야 하는 사례다. raw score와 분리해 읽는다.
- **문항을 늘리면 grounding 감사를 자동화해야 한다.** 현재 m2·m3의 결함은 손으로 원문을 대조해 찾은 것이라 문항 수가 늘면 같은 방식이 통하지 않는다. 다만 문자열 포함 검사로는 잡히지 않는다 — `Denver`도 `m030`에, `Minneapolis`도 `m042`에 실제로 들어 있고, 틀린 것은 문자열이 아니라 **관계**다. 따라서 step당 LLM 판정이 필요하며(`tools/audit_grounding.py` 자리), 문항 확장 시점에 만든다.
- 확장으로 데이터 caveat이 커지면 README 해석 경계에서 `data/<dataset>/NOTES.md`로 분리한다. 현재는 문항 3개·결함 2건이라 README 절 하나로 충분하다.
