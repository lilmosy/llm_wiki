__pycache__/
*.pyc
runs/*.log
.env
.venv/
.DS_Store

# 실행하면 다시 만들어지는 것들 (커밋하지 않는다)
# LLM 응답 캐시. 재실행 비용을 줄일 뿐, 결과의 근거가 아니다
runs/cache/
# 과거 실행 snapshot. 버전 보관은 git이 한다
runs/history/
# 임베딩 벡터. 로컬 Qwen이 API 비용 없이 재생성한다
*.npz
